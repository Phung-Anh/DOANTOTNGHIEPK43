@model IEnumerable<DOANTOTNGHIEPK43.Models.EF.ProductCategory>

@if (Model != null && Model.Any())
{

    <div class="banner">
        <div class="container">
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div class="banner_item align-items-center" style="background-image: url(@item.Icon) ">
                            <div class="banner_category">
                                <a href="@item.Alias">@item.Title</a> @*với đường dẫn sẽ là dữ liẹu cảu alias trong product category*@
                            </div>
                        </div>
                    </div>

                }

                @*<div class="col-md-4">
                    <div class="banner_item align-items-center" style="background-image: url(/Content/assets/images/bomypham5mon.jpg)">
                        <div class="banner_category">
                            <a href="categories.html">unisex</a>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="banner_item align-items-center" style="background-image: url(/Content/assets/images/bomyphamchonam.jpg) ">
                        <div class="banner_category">
                            <a href="categories.html">men's</a>
                        </div>
                    </div>
                </div>*@
            </div>
        </div>
    </div>
}
